en:
  theme_metadata:
    description: Display text ads between topics.
    settings:
      show_between_every:
        description: "Number of topics between ads"
      ads:
        description: "Ad content, links, and visibility"
        schema:
          properties:
            link:
              description: "Destination URL"
            text:
              description: "Content of the ad"
            include_groups:
              description: "Use groups to control who will see the ads. Leave blank for all. Format as a comma-separated list of group names. Included 'anon' for logged-out visitors."
            exclude_groups:
              description: "Exclude members of these groups. Format as a comma-separated list of group names."
            custom_class:
              description: "Comma-separated custom CSS classes for the ad"
